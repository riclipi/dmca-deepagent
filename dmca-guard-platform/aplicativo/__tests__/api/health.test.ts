describe('/api/health', () => {
  // Skip tests for now until we fix the Next.js request mocking
  it.skip('health check endpoint tests', () => {
    // TODO: Fix Next.js request mocking for API route tests
  })
})